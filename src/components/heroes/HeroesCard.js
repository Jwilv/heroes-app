import React from 'react'

export const HeroesCard = ({
    id,
    superhero, 
    publisher, 
    alter_ego,
    first_appearance,
    characters,
}) => {
  return (
    <div>
        <h1>{superhero}</h1>

    </div>
  )
}
